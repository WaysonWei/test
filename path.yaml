version: '3.1'
services:
  webapp:
    env_file:
      - stack.env
    image: nginx:alpine
    restart: always
    ports:
      - ${ext-port}:$port
    volumes:
      - ./static:/usr/share/nginx/html
